export enum LoginRequestStatus {
  IDLE = "idle",
  PENDING = "pending",
  FAILURE = "failure"
}

export enum LogoutRequestStatus {
  IDLE = "idle",
  PENDING = "pending",
  FAILURE = "failure"
}

export enum SignupRequestStatus {
  IDLE = "idle",
  PENDING = "pending",
  FAILURE = "failure",
  SUCCESS = "success"
}
