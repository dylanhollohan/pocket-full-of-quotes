import { userSlice } from './reducers';

export const {
  signupRequest,
  signupSuccess,
  signupFail,
  resetSignup,
  loginRequest,
  loginSuccess,
  logoutRequest,
  logoutSuccess,
} = userSlice.actions